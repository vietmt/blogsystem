<%= form_for([@entry, @entry.comments.build], :html => {:style => 'display: none;'}) do |f| %>
  <div class="field">
    <%= f.label :content, t('comments.body') %><br />
    <%= f.text_area :body %>
  </div>
  <div><%= hidden_field_tag :user_id, current_user.id %></div>
<% end %>